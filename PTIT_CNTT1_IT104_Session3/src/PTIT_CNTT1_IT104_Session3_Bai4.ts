const a: string = '2';
const b: number = 2;

// if (a === b) console.log('a===b ->true');//false Vì kiểu dữ liệu khác nhau (string !== number) → false.
// else console.log('a===b -> false');

// if (a == b) console.log('a==b ->true');
// else console.log('a==b -> false'); //true Vì nó sẽ ép kiểu tự động trước khi so sánh

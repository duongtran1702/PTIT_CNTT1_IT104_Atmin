"use strict";
constructor(accountNumber, string, balance, number, history, HistoryTransaction[], status, string, public, interestRate, number);
{
    super(accountNumber, balance, history, status);
}
